from ursina import * #Import the default application settings

#Application settings
window.title = 'My Game' #Set the application's title
window.exit_button.enabled = False #Disable the visible exit button